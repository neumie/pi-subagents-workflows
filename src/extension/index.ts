import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Scaffold only: workflow tools and commands are intentionally not registered yet. */
export default function piSubagentsWorkflowsScaffold(
  _pi: ExtensionAPI,
): void {}
